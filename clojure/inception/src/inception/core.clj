(ns inception.core
  (:gen-class))

; Write the function `inception` that makes the program output the
; text "Metalevel excursion.".

; Do _not_ edit anything below.
(defn -main
  [& args]
  (println (((((((inception "Metalevel excursion.")))))))))
